# Agent notes

This repo’s standing context for Cursor lives in:

- `.cursor/rules/fcn-ensembling-overview.mdc` — map, GPU, tmux
- `.cursor/rules/fcn2-terminology.mdc` — W0 vs A, VGA mixture
- `.cursor/rules/fcn2-erf-hidden-kernel.mdc` — training dirs, snapshots, VGA CLI
- `.cursor/skills/fcn2-research-context/` — read when the task is VGA / bimodality / steepwell

Do not re-derive that map from a whole-repo search unless those files are missing or the user says they are stale.
